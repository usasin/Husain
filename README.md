# PRONO4 iOS — BUILD74 source update

Source: `PRONO4_BUILD74_GAME_LOOP.zip`, version `2.6.18+74`.
Prepared on 22 September 2026 from `usasin/Husain:prono4-build41`
at `5a2370ef417ab0c1743f3e2c2d460c0dbe5a347d`.

## Status

Source preparation only. No Codemagic build, TestFlight upload, App Store
submission, or Firebase deployment is performed by this update.
The workflows in this branch have no automatic triggers. The preparation
commit includes `[skip ci]`. Start a build manually only when requested.

The committed `lib/`, `assets/`, and `pubspec.yaml` are the BUILD74 source.
The old BUILD50/BUILD55 restoration scripts and archived overlays remain for
history but are no longer called by either workflow. Do not run them over
this source.

## iOS integration retained

- App Store application: `6809793049`.
- Bundle ID: `com.digitalsolutionsai.prono4`.
- iPhone/iPad support and iOS 15 minimum, Flutter 3.44.8.
- Existing signing integration, shared distribution certificate, app-specific
  provisioning profile, and Firebase secret group are retained.
- The native iOS host is generated by the existing `PREPARE_IOS_IPAD.sh`.
  The ZIP's `com.mundial.app` iOS host is not imported.
- iOS Firebase continues to use the validated `GoogleService-Info.plist`
  injected during preparation and included in Runner's resources.
- ATT is requested before UMP/AdMob; existing production iOS ad unit IDs
  are retained. BUILD72 rewarded-analysis logic is preserved.
- Visible Block/Report controls are reapplied to both chats; current reputation
  badges and BUILD74 screens are retained.

The source build number is 74. The existing manual release workflow selects
the next TestFlight build number at build time, so these numbers need not match.

## Changes included

BUILD74 adds daily missions, XP, levels, participation streaks, four-week
seasons, results recaps and teammate challenges. BUILD73 EURO and notification
changes, and earlier BUILD72 fixes, are included.

The Firebase source and deployment scripts are staged only. BUILD73 server
changes and BUILD74 challenge rules must be reconciled with production and
deployed separately if not already present. The supplied Firestore rules do
not define `contentReports`; preserve/verify the production moderation rules
before any future rules deployment. Do not blindly overwrite production rules.

## Validation

Run `python3 scripts/VERIFY_BUILD74_SOURCE.py` for source integration guards.
The preparation environment checks Dart syntax, imports, YAML and shell syntax.
Flutter analysis, iOS compilation, signing, device tests and backend integration
remain pending the later authorized build/test stage.
